import React from "react";

export default function (props) {
  return (
    <div className="square-main-container">
      {/* {props.value} */}
      <div className="individual-grid">{props.value}</div>
    </div>
  );
}
